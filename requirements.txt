aiogram==3.10.0
python-dotenv==1.0.0
aiohttp==3.9.1
asyncpg==0.29.0
aiosqlite==0.19.0
psycopg2-binary==2.9.9
# Optional but helpful for production:
uvloop==0.19.0  # Faster asyncio event loop (Linux only)
pytz==2023.3    # Timezone support
